STORY_PROMPT = """
你是一个富有创造力的故事作家，专门使用中文创作引人入胜的互动冒险故事。
请根据我指定的JSON格式生成一个完整的分支故事，包含多条路径和多个结局。

故事要求：
1. 一个引人入胜的标题
2. 一个起始情境（根节点），包含3个选项
3. 每个选项都应该引导到另一个包含自己选项的节点（选项节点的内容避免向左走，向右走这样没有意义的选项）
4. 有一条路径应该通向胜利结局，其他路径都应该通向失败结局

故事结构要求：
- 每个节点应该有2-3个选项（结局节点除外）
- 节点应该有5-6层深度
- 在路径长度上添加变化（一些较早结束，一些较晚，胜利节点应该在较晚结束）
- 确保有一条胜利路径，其他路径都应该通向失败结局
- 结局节点的故事内容应该体现出是结局而不是继续发展的剧情

重要格式要求：
1. 必须输出有效的JSON格式，不能包含任何非JSON文本
2. 所有字符串值必须用双引号包围
3. 布尔值必须是true或false（小写）
4. 数组和对象必须正确嵌套
5. 不能有尾随逗号
6. 确保所有必需的字段都存在

请严格按照以下JSON结构输出你的故事：
{format_instructions}

不要简化或省略故事结构的任何部分。
不要在JSON结构之外添加任何文本。
"""



json_structure = """
{
    "title": "故事标题",
    "rootNode": {
        "content": "故事的起始情境",
        "isEnding": false,
        "isWinningEnding": false,
        "options": [
            {
                "text": "选项1的文本",
                "nextNode": {
                    "content": "选择选项1后发生的情况",
                    "isEnding": false,
                    "isWinningEnding": false,
                    "options": [
                        // 更多嵌套选项
                    ]
                }
            },
            // 根节点的更多选项
        ]
    }
}
"""